---
- hosts: all
  tasks:
    - name: COLLECT BIG-IP FACTS
      bigip_device_info:
        gather_subset:
        - system-info
        provider:
#          password: "{{ ansible_ssh_pass}}"
          server: 192.168.1.241
#          user: "{{ ansible_user }}"
      register: device_facts